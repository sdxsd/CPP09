#include "BitcoinExchange.hpp"

int main(void) {
	// std::ifstream x;
	// x.open("data.csv");
	// std::ifstream zoip;
	// zoip.open("input.txt");
	// BitcoinExchange exchange(x);
	// exchange.parseInput(zoip);
}
